#This assumes that the program is launched from the main dir of the project
_standartConfigPath = ".config\\config.json"

from .config import Config as Config
from .ConfigTypes import ConfigTypes as ConfigTypes